 //TODO temp
        if (displayName == password && displayName == "admin")
        {
            return new User
            {
                DisplayName = displayName,
                FolderName = "admin",
                IsAdmin = true,
                PasswordHash = "admin"
            };
        }